services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gahoi
      POSTGRES_PASSWORD: gahoi_dev
      POSTGRES_DB: gahoisarthi
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
